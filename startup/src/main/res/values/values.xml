<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 无限等待 -->
    <item name="infinite_await" type="integer">-1</item>
    <!-- 不等待 -->
    <item name="no_await" type="integer">0</item>
    <!--  默认等待10秒  -->
    <item name="default_await" type="integer">10000</item>

    <!-- 自动生成别名 -->
    <item name="bf_auto_alias" type="string">$$</item>
    <!-- 不设置依赖项 -->
    <item name="bf_no_dependencies" type="string">$$</item>
</resources>